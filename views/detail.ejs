<h1><%=  movie.Title %></h1>
<p><%=  movie.Year %></p>
<h4><%=  movie.Rated %></h4>
<h6><%=  movie.Genre %></h6>
<p><%=  movie.Director %></p>

<img src="<%= movie.Poster %>" />

<!-- form to CREATE a new fave -->
<form method="POST" action="/faves">
    <input hidden value="<%= movie.Title %>" name="title"/>
    <input hidden value="<%- movie.imdbID %>" name="imdbid"/>
    <button type="submit">Fave this movie</button>

</form>